/**
 * Vitest test setup — runs once before all frontend tests.
 *
 * Adds @testing-library/jest-dom matchers like `toBeInTheDocument`,
 * `toHaveTextContent`, etc. to expect().
 */
import '@testing-library/jest-dom/vitest';
